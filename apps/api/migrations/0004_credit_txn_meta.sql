-- =====================================================================
-- 0004 — credit_transaction.meta (백로그 M4)
-- 소비 시 부여분/구매분 분배 내역을 기록 → 취소 환원 시 원래 버킷으로 복원.
-- =====================================================================

ALTER TABLE credit_transaction
  ADD COLUMN IF NOT EXISTS meta JSONB;
