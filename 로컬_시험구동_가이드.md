# 잇올 멘토링 — 계정·구독 없이 로컬 시험구동 가이드

> 목적: **외부 결제/알림톡/줌/클라우드 계정 없이**, 보유 기기(Mac mini·Windows PC·Galaxy S23 Ultra·iPad mini)에서
> 실제로 구동·검증할 수 있는 영역을 정리. 아래 ✅ 표시는 **이 문서 작성 시 라이브 호출로 직접 확인**한 항목.
> 최종 확인: 2026-06-30

---

## 0. 한 줄 요약
- 모든 외부 의존은 **mock/local 어댑터**라, **계정 없이도 앱의 거의 모든 흐름을 끝까지 돌려볼 수 있음**.
- "형태만" 보여주는 건 **실결제 승인·줌 입장링크·SMS/알림톡 발송·AI 검토** 4가지뿐(아래 §4 구분).

---

## 1. 어떤 기기로 무엇을 (계정 0)
| 기기 | 역할 |
|---|---|
| **Mac mini** | 백엔드(Docker: Postgres·Redis·API) + 웹 + iOS 빌드/시뮬레이터까지 단독 가능 — **메인 머신 추천** |
| **Windows PC** | 백엔드·웹·Android 개발 가능(단 iOS 빌드는 불가) — 보조 머신 |
| **Galaxy S23 Ultra** | 실 Android 기기에서 학생/학부모 모바일앱 (Expo Go) |
| **iPad mini** | 실 iOS 기기에서 모바일앱 (Expo Go) |

---

## 2. 구동 절차

### 2-1. 백엔드 (Mac mini 또는 PC)
```bash
cd itall-mentoring
docker compose up -d                 # postgres + redis (계정 불필요)
cd apps/api
cp .env.example .env                 # 이미 있으면 생략
npm ci                               # 최초 1회
npm run db:init                      # 마이그레이션(원장) + prisma generate
npm run seed                         # 더미 계정·정책 시드
npm run start:dev                    # → http://localhost:3000/api/v1
```
헬스 확인: `curl http://localhost:3000/api/v1/health` → `{"status":"ok","checks":{"db":"up"}}` ✅

### 2-2. 웹 (선생님·관리자·HR)
```bash
cd apps/web && npm ci && npm run dev   # → http://localhost:5173
```
브라우저에서 시드 계정으로 로그인(§3).

### 2-3. 모바일 (학생·학부모) — 실기기 Expo Go
1. **S23 / iPad**에 스토어에서 **Expo Go** 설치(무료, 계정 불필요).
2. 폰/패드와 Mac을 **같은 와이파이**에 연결.
3. 모바일 앱이 바라볼 API 주소를 **Mac의 LAN IP**로 지정 — `apps/mobile/app.json`:
   ```json
   "extra": { "apiBase": "http://192.168.x.x:3000/api/v1" }
   ```
   (Mac IP 확인: 터미널 `ipconfig getifaddr en0` — 예: `192.168.100.119`)
4. 백엔드 CORS 허용(`apps/api/.env`): `CORS_ORIGINS=*` (로컬 시험용)
5. 실행:
   ```bash
   cd apps/mobile && npm ci && npx expo start
   ```
   터미널 QR을 **Expo Go로 스캔** → 실기기에서 즉시 구동.

> 참고: 앱이 `expo-secure-store`(Expo Go 내장)만 쓰므로 **별도 dev build 불필요**.

---

## 3. 시드 더미 계정 (PW 공통: `dev-password!`)
| loginId | 역할 | 비고 |
|---|---|---|
| `student01` | 학생 | 모바일 |
| `guardian01` | 학부모 | 모바일 |
| `teacher01` | 선생님 | 웹 |
| `admin01` | 센터 관리자 | 웹 |
| `hr01` | 인력관리(HR) | 웹 |
| `hq01` | 본사 슈퍼관리자(HQ) | 웹 |

---

## 4. mock 어댑터 — "실제 동작" vs "형태만"
| 영역 | 상태 | 설명 |
|---|---|---|
| DB·인증·권한 | **실제** | Postgres 실데이터, JWT 로그인/회전/로그아웃 |
| 예약 흐름(휴게버퍼·요금·크레딧 소비·상태머신) | **실제** | 도메인 규칙 끝까지 동작 |
| 파일 첨부 | **실제** | 로컬 디스크에 실제 저장/다운로드 |
| 인앱 알림 수신함 | **실제** | notification 기록·조회·읽음 |
| 캐시·rate limit | **실제** | 메모리(또는 Redis) 기반 |
| 크레딧 주간부여·구독 정기결제(autopay) | **실제 흐름** | 스케줄러·결제주기·이월 동작 (단 결제 승인만 mock) |
| 💳 실결제 승인 | **형태만** | `MockPgProvider` 항상 승인 — 실제 돈 안 나감 |
| 🎥 줌 입장 URL | **형태만** | `https://meet.local/itall/{id}` placeholder |
| 📱 SMS / 카카오 알림톡 발송 | **형태만** | 미발송, delivery=`failed`로 기록(재시도 큐에 남음) |
| 🤖 AI 신고검토·답변 유사도 | **형태만** | 키워드 stub |

→ "형태만" 4가지는 **계정/키가 정해지면 어댑터만 교체**(코드 변경 없음, §10).

---

## 5. 계정 없이 검증 가능한 기능 체크리스트
**인증/보안** (✅ = 라이브 확인됨)
- [x] 로그인 → access/refresh 발급 ✅
- [x] `/me` 본인 조회 ✅
- [x] refresh 토큰 회전(이전 토큰 재사용 시 401) ✅
- [x] 로그아웃 후 refresh 무효
- [x] rate limit(로그인 분당 10회 초과 → 429) ✅
- [x] 약한 비밀번호 가입 거부(400) ✅

**예약/상담 (학생·선생님)**
- [ ] 검색 → 요금견적 → 예약 생성(휴게버퍼·요금·크레딧 차감)
- [ ] 상태 전이(수락/확정/완료/취소/노쇼)
- [ ] zoom 예약 확정 시 입장 URL(placeholder) 발급
- [ ] 상담 기록 임시/최종 저장·공개정책
- [ ] 역상담 제안/수락
- [x] 파일 첨부 업로드/다운로드(로컬 실저장) ✅

**관리자/HR/학부모**
- [ ] 관리자 정책(요금·한도·기능토글·줌·상담실·차단)
- [ ] HR 학생 등록·승인
- [ ] 학부모 연결·공개정책·결제요청
- [ ] 구독·등급·주간 크레딧 부여(스케줄러)
- [ ] 구독 정기결제(autopay, mock 승인)

**확장(Phase 3)**
- [ ] 온라인 Q&A·채택, 랭킹/리뷰, 신고/차단(AI 검토 stub), 급여 정산

---

## 6. 빠른 스모크 점검(터미널) — 모두 라이브 확인됨 ✅
```bash
B=http://localhost:3000/api/v1
curl -s $B/health
# 로그인 → 토큰
LOGIN=$(curl -s -X POST $B/auth/login -H 'Content-Type: application/json' \
  -d '{"loginId":"student01","password":"dev-password!"}')
# 파일 업로드/다운로드, refresh 회전, rate limit 등은 본 가이드 작성 중 직접 확인함
```

---

## 7. 이건 기기로 안 됨(= 계정/구독 필요, 하드웨어 문제 아님)
- 실 결제(Toss/PortOne), 카카오 알림톡/SMS 키, Zoom 계정
- iOS 푸시(APNs=Apple Developer $99/년) / Android 푸시(FCM=무료, Firebase 프로젝트)
- 앱스토어·플레이스토어 배포, 클라우드 호스팅·오토스케일·시크릿 매니저
