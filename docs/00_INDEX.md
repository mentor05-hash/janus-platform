# 야누스 프로젝트 폴더 — 00_INDEX (단일 진입점)

갱신 2026-07-31 · 이 파일이 폴더의 지도이자 규칙이다. 새 세션(클로드/클로드 코드)은 이 파일부터 읽는다.

> **새 세션은 이 파일 다음에 `20_exec/야누스_세션인계_2026-07-31.md` 를 읽어라.** 저장소 상태(base tip · 워킹트리 위치) · 가동 중인 자동화 · 작업 규약(원격 판정 · check-runs · U+00B7 · lock 함정) · 열린 일 우선순위가 거기 있다.

---

## 1. 폴더 구조와 git 정책

```
janus/
├── 00_docs/            기획·실행 문서 전체 → janus-platform repo의 docs/로 편입 (git O)
│   ├── 10_master/      상위 기획: 사업종합기획서(현재 v2) · 확장청사진
│   ├── 20_exec/        살아있는 문서: 12주 실행계획서(매일 체크) · Decision-Register
│   ├── 30_features/    기능 기획서 (현재: QnA·커뮤니티·리그. 이후 기능당 1파일)
│   ├── 40_design/      디자인 브리프·핸드오프 (⚠영문 파일명만 — Design 툴 호환)
│   └── 90_archive/     구버전·이관된 원본 보고서 (수정 금지, 참조만)
├── 10_platform/        janus-platform 모노레포 clone (git O, private)
├── 20_data/            ⚠저작권·민감 데이터 전용 — git remote 연결 절대 금지
├── 30_public/          janus-public (마케팅·무료 배치표 → Cloudflare Pages, git O public)
└── 40_workbench/       임시 산출물·시안·스크린샷 (git X, 매주 일요일 정리)
```

## 2. 저장 규칙 (작업물을 "바로바로" 저장하는 법)

1. **모든 산출물은 생성 즉시 위 5폴더 중 하나로.** 어디인지 3초 안에 답이 안 나오면 `40_workbench/`에 넣고 일요일 리뷰에서 승격(00_docs)하거나 삭제한다 — 방치 금지.
2. **파일명 규약**: `주제_버전_YYYY-MM-DD.md` (예: `야누스_QnA_커뮤니티_리그_기획서_2026-07-07.md`). 디자인 폴더만 영문.
3. **살아있는 문서는 항상 1개**: 실행계획서·Decision-Register·각 기획서는 최신본만 제자리에 두고, 버전 갱신 시 구버전을 즉시 `90_archive/`로 이동. "v2가 어디 있더라" 검색이 필요 없는 상태 유지.
4. **문서의 서열**: 종합기획서(전략) > 확장청사진(장기 지도) > 실행계획서(전술·체크) > 기능 기획서(명세) > 디자인 브리프. 충돌 시 상위 문서가 이기고, 하위를 고친다.
5. **매 세션 종료 리추얼**(실행계획서 §4와 동일): 산출물 제자리 저장 → 커밋+push(00_docs 포함) → 실행계획서 체크 갱신.

## 3. 클로드 프로젝트(웹) 지식과의 동기화

- 클로드 프로젝트 지식에는 **최신 4종만** 유지: 종합기획서 v2 · 실행계획서 v2 · QnA 기획서 · 확장청사진. (기존 잇올 기능총괄보고서·야누스 종합보고서·디자인프롬프트 3종은 본 폴더 `90_archive/legacy_reports/`로 이관 완료 — 프로젝트 지식에서 교체 권장.)
- 문서를 v-업 할 때마다 프로젝트 지식도 같은 날 교체(일요일 리뷰 체크 항목).

## 4. 현재 문서 지도 (2026-07-31 기준)

| 문서 | 위치 | 상태 |
|---|---|---|
| 야누스 통합 사업종합기획서 **v2** | 10_master | 확정(비전 문구 v2.1 반영) |
| 야누스 확장청사진 (버티컬·글로벌) | 10_master | 확정 — 트리거 T1~T6 대기 |
| 야누스 12주 실행계획서 **v2** | 20_exec | **활성** — 매일 체크 (W1 = 7/7~) |
| Q&A·커뮤니티·리그 기획서 v1.1 | 30_features | 확정 — W5~ 구현 |
| UI 전면 개편 브리프 (7/8 클로드 디자인) | 40_design | 실행 완료(7/13 전면 리스킨 반영) |
| Decision-Register | 20_exec | **활성** — O1~O201 · N1~N37 (최신: O198~O201) |
| 구 잇올/야누스 보고서 3종 | 90_archive/legacy_reports | 참조 전용 |
| **배치표 핸드오프(C1~C6 접합계약)** | 30_features | **동결 계약** — 위반 금지(2026-07-14) |
| 정시배치표 이론서(컨설턴트용)·아우구르 방법론 | 30_features | 참조·교육용 |
| Q&A 코드 감사(W5 D1 선행) | 30_features | 감사 완료 — 갭 백로그 |
| 디자인 통합: 감사 매트릭스·토큰 추출·백로그 | design-integration | 스텝 0(a항)·1 완료 · 스텝 4 백로그 활성 |
| 무료판 배포 전 점검표 | 20_exec | 배포 직전 체크용(W2 D6) |
| **세션 인계 2026-07-31** | 20_exec | **새 세션 필독** — 상태·규약·열린 일 |
| 브랜치 보호 설정 런북 | 20_exec | 발효 판정법(`isRequired`) · `Require approvals` 교착 함정 |
| 백업 운용 런북 | 20_exec | 위협 모델 · 외장 분리 운용 · 복원 리허설 주기 |
| DB 리네임 마이그레이션 런북 | 20_exec | 덤프·복원 절차(복원 리허설이 이걸 따른다) |
| janus_score 변환 스펙 **v1.1** · SSO 설계 v1 | docs 루트 | 구현 완료(플랫폼측) — C1·O42 |
| 배치표 허브 운영 가이드 | (repo) ops/placement/ | JANUS_DATA_DIR 배치·티어 게이트 |

## 5. 다음 액션 (이 폴더 관련)

- [x] ~~skeleton 배치 · `00_docs` 를 repo docs/ 로 편입 후 첫 push~~ (W1 D1 완료)
- [x] ~~`20_exec/Decision-Register.md` 생성~~ (완료 — 현재 O201)
- [ ] 클로드 프로젝트 지식 4종 교체 (§3)

**열린 일의 우선순위는 이 파일이 아니라 `20_exec/야누스_세션인계_2026-07-31.md` §4 를 본다** (도메인 구입 → W2 D2 무료판 마감 → 외장 디스크). 실행계획서 체크박스가 1차 사실원이고, 인계 문서는 '지금 무엇이 막혀 있는가'를 요약한다.
