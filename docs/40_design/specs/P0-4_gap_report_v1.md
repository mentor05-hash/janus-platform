# P0-4 · 격차 리포트 스펙 시트

> **[draft] 토큰·정본 확정 시 개정.** 원본 목업: `40_workbench/janus_gap_report_v1.dc.html`.
> §2-B 격차 리포트 **P0-4 · 신규**. 라우트: `/report/gap` (신규 — App.tsx 1:1 대조 확정 예정). 진입점: 성적 추이.

## 개념
현재 위치 → 목표까지의 격차를 한 화면에서. 구성: 현재 위치 카드 → 격차 바(과목별) → 추세 → **처방 CTA**(선생님/상담으로). 학생·학부모 공용 가독(본문 ≥15/17px).

## §1 GatewayCard 필드표 (코드 1:1 — 공용 3곳 중 하나)
| 필드 | 타입 | 예시 |
|---|---|---|
| `title` | string | "이 격차, 이렇게 좁혀요" |
| `description` | string | "수학 3등급 → 목표 1등급, 추천 처방" |
| `service` | enum | `prescription` |
| `href` | route | `/teachers?gap=math` |
| `ctaLabel` | string | "선생님 매칭 보기" |
> 관문홈·격차리포트·학부모리포트 공용. 처방 CTA가 GatewayCard로 렌더.

## 신호등 4구간 (판정)
격차/도달 가능성 표시에 안정 `#2a8a5f` · 적정 `#57a86a` · 소신 `#cf9f2f` · 상향 `#d06b52`. 빨강 남발 금지.

## 배지 (§1)
- 근거 배지: `source: llm|rules`(추천 근거) · 성적연동 배지 · 신뢰도 `relTier: A|B|C`.
- 수치 옆에 항상 근거를.

## CTA 위계 (골드 1개)
- 골드 채움 = 처방 CTA(다음 문) — `data-janus-cta="gap_prescribe"`.
- 블루 = 상세 리포트/추세 보기 보조.

## 반응형
데스크톱 2열(위치+격차 / 추세+처방). 모바일 1열 스택. 격차 바는 375px에서도 라벨 가독.

## self-check 7종
① 375px 가독 ✅ · ② AI 라벨(추천 근거 llm 표기) ✅ · ③ 배지 3종 ✅ · ④ CTA id ✅ · ⑤ "잇올" grep 0 ✅ · ⑥ 영문 파일명 ✅ · ⑦ 다크모드 ✅

## 썸네일
`thumb/P0-4.png` (비식별 — 실명·실점수 마스킹)
