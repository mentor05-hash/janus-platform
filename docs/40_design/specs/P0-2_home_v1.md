# P0-2 · 관문 홈 스펙 시트

> **[draft] 토큰·정본 확정 시 개정.** 원본 목업: `40_workbench/janus_home_v1.dc.html` (정본). 변형 `home_v2` = drop.
> §2-A 관문 홈(랜딩) **P0-2 · 개편**. 라우트: `/` (실측: apps/web `SiteLandingPage`, App.tsx 1:1 대조 확정 예정).

## 개념
역할 선택을 강요하지 않는 "관문 우선" 진입. 중앙 큰 질문 + 자유서술 입력 → 야누스가 지난 입시 데이터를 읽어 "다음 문"을 제시. 하단 액션 타일(GatewayCard). 데스크톱+모바일 반응형.

## ⚠ [DEC] 히어로 2안 병기 — 단일 확정 금지
두 방향 모두 유지. 최종 택일은 별도 결정.
| 안 | 출처 | 톤 |
|---|---|---|
| **A · 딥 네이비 포탈** | 랜딩 시안 2d | `radial-gradient(#1b4a7e→#0a1728)` 밤하늘 포탈·별·빛기둥. 몰입·프리미엄 |
| **B · 여명 라인** | 랜딩 시안 2c | 밝은 배경·라인 아치·여명. 경쾌·명료 |
- 썸네일 2종: `thumb/P0-2_deepportal.png` · `thumb/P0-2_dawnline.png`
- 히어로 카피 공통: "무엇을 준비하고 있나요?" + 자유서술 인풋 + "길 찾기 →"(골드 CTA).

## §1 GatewayCard 필드표 (코드 1:1 — 이 화면이 공용 3곳 중 하나)
| 필드 | 타입 | 예시 | 비고 |
|---|---|---|---|
| `title` | string | "격차 리포트 받기" | 카드 제목 |
| `description` | string | "지금 위치와 목표까지의 거리" | 본문 |
| `service` | enum | `diagnosis` | 도메인(색·아이콘 결정) |
| `href` | route | `/report/gap` | 이동 경로 |
| `ctaLabel` | string | "무료로 확인" | 버튼 라벨 |
> 관문홈·격차리포트·학부모리포트가 GatewayCard 공용. 세 화면 필드 동일 계약.

## CTA 위계 (화면당 골드 1개)
- 골드 채움 = 히어로 "길 찾기 →" (핵심 전환) — `data-janus-cta="home_hero_search"`
- 블루/회색 = 액션 타일 보조 CTA. 각 타일 버튼에 `data-janus-cta` id.

## 배지·라벨
타일에 신호등/근거 배지 노출 시 §1 계약 준수(`source`·`relTier`). AI 관련 카피엔 라벨 3종 색 적용.

## 반응형
데스크톱 히어로 600px, 3열 타일. 모바일 390px 프레임, 히어로 세로 포탈, 타일 1열. 본문 ≥15px.

## self-check 7종
① 375px 가독 ✅ · ② AI 라벨 ✅ · ③ 배지 3종 ✅ · ④ CTA id ✅ · ⑤ "잇올" grep 0 ✅ · ⑥ 영문 파일명 ✅ · ⑦ 다크(히어로 포탈 자체 다크) ✅
