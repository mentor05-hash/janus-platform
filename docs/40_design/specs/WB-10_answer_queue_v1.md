# WB-10 · 답변 큐(공급자) 스펙 시트

> **[draft] 토큰·정본 확정 시 개정.** 원본 목업: `40_workbench/janus_answer_queue_v1.dc.html` (다크).
> §2-D 답변 큐 · 개편. 라우트: `/answer/queue` (App.tsx 1:1 대조 예정). 선생님 데스크톱 밀도.

## 개념
선생님이 받은 질문을 SLA 순으로 처리하는 큐. 다크 서피스·정보 밀도·큐 처리 효율 우선.

## AI 투명성 (§1 · 다크)
- ✦AI 초안 라벨 = 다크용 밝은 바이올렛 `#9a8be8`(bg rgba(154,139,232,.18)).
- 각 항목 상태(대기/초안생성/작성중). 출처 `source: llm|rules`.
- SLA 잔여시간·풀별 평균 응답시간 모노 수치.

## CTA 위계 (다크 골드 1개)
- 골드 채움(다크 `#e3b45c`) = "답변 작성" `data-janus-cta="queue_answer"`.
- 블루(다크 `#4d93da`) = 미리보기·필터 보조.

## 반응형
데스크톱 우선(밀도). 태블릿까지. 큐 행 클릭→답변 에디터(WB-11).

## self-check 7종
① 375px 가독(태블릿 하한) ✅ · ② AI 라벨 다크 `#9a8be8` ✅ · ③ 배지 3종 ✅ · ④ CTA id ✅ · ⑤ "잇올" grep 0 ✅ · ⑥ 영문 파일명 ✅ · ⑦ 다크(기본 다크) ✅

## 썸네일
`thumb/WB-10.png` (비식별 — 학생 질문 마스킹)
