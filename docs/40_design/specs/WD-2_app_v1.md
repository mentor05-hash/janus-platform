# WD-2 · 모바일 앱 셸 스펙 시트

> **[draft] 토큰·정본 확정 시 개정.** 원본 목업: `40_workbench/janus_app_v1.dc.html`.
> §2-B/C 모바일 셸(하단 탭 5개). 라우트: `/app` 셸(App.tsx 1:1 대조 예정).

## 개념
모바일 하단 탭 5개 고정: 홈(관문)·질문(Q&A)·진단(배치표/리포트)·일정(상담·수업)·내정보. 새 서비스는 홈 커리큘럼 카드·허브로 흡수 — 탭 안 늘림.

## 구성 (§1)
- 탭 아이콘·라벨. 활성 탭 위계 색. 뱃지(미읽음) 신호.
- 상단 컨텍스트 바(역할·알림). 화면 전환은 셸 유지.

## CTA 위계
- 탭 = 내비(위계 색). 화면별 골드 CTA는 각 화면 소유(셸은 중립).

## 반응형
모바일 전용. 탭 터치타깃 ≥44px. safe-area 대응. 본문 ≥15px.

## self-check 7종
① 375px 가독 ✅ · ② AI 라벨 N/A(셸) ✅ · ③ 배지(미읽음) ✅ · ④ CTA id(탭) ✅ · ⑤ "잇올" grep 0 ✅ · ⑥ 영문 파일명 ✅ · ⑦ 다크모드 ✅

## 썸네일
`thumb/WD-2.png` (비식별)
