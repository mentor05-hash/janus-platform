# 디자인 통합 백로그 (지시서 스텝 4 — 개발 금지, 문서만)

> 작성 2026-07-13 · 시안 번들(125화면)·최신 홈(janus_web2d_v1)·배치표 핸드오프에서 식별된 **미구현 신규 기능** 등록.
> 규칙: 각 항목은 주간 리뷰에서 [DEC] 후 주차 배치. 예상 규모 = 클로드 코드 세션 수(1세션 ≈ 반나절).

## 1. 지시서 명시 4종 (스텝 4 원안)

| 항목 | 시안 근거 | 예상 규모 | 의존성 | 주차 제안 |
|---|---|---|---|---|
| **포트폴리오(가·나·다 조합 자동 추천 + 종합 합격확률)** | janus_portfolio_v1 · report_hub의 조합 A/B 탭 | 4~6세션 | 배치표 엔진(독립 트랙)·유료 티어 | **W9(유료 티어)와 함께 [DEC]** — 킬러 기능 후보 |
| 지원선 배치 맵/스펙트럼 | janus_placement_proto_v1 | 2~3세션 | 배치표 데이터 | 회원 티어 후보(W3~4 이후) |
| 온보딩("성적 없이 예시로 둘러보기") | janus_onboarding_v1·tutorial_v1 | 1~2세션 | 없음(웹 단독) | **W6 소프트 런칭 전 소형 태스크 추천** |
| 컨설턴트·관리자 콘솔(gbias·원본 컷·학생 프로파일) | janus_consultant_console_v1·data_console_v1 | 5~8세션 | 컨설턴트 티어(CF Access, N25)·Phase 2 | Phase 2 |

## 2. 이번 통합에서 추가 식별 (시안 존재·기능 부재)

| 항목 | 시안 근거 | 예상 규모 | 의존성 | 비고 |
|---|---|---|---|---|
| AI 튜터 채팅 | janus_ai_tutor_chat_v1 | 3~4세션 | LlmProvider(키)·비용 상한 | Q&A 트랙(Q1~Q5)과 결합 검토 |
| 커뮤니티 리그(승급·질문가·보상) | janus_community_v1 + 기획서 §리그 | 4~6세션 | Q&A 트랙 | W8 리그 수치 [DEC](N27) 선행 |
| 학부모 모바일 요약·리포트 | janus_parent_mobile_v1·parent_report_v1 | 2~3세션 | 격차 리포트 v1(W4) | 원점수 잠김 규칙 포함 |
| 버티컬 팩 7종(멘탈·커리어·습관·수면·체력·플래너·입시캘린더) | janus_packs_v1 총람 + 각 v1 | 팩당 1~2세션 | 대시보드 슬롯 구조 | 등록 규격 {icon,color,bg} — 토큰은 이미 준비됨(`--j-dom-*`) |
| 랜딩 실연동(강좌·공지·인기 학과·신뢰 지표) | janus_web2d_v1 | 2세션 | 강좌·공지 모듈, funnel 집계 | 현재 '예시' 라벨 상태(O51) |
| 소셜 로그인(카카오·전화·Apple) | janus_login_v1 | 2~3세션 + [EXT] 키 발급 | OAuth 앱 등록 | "역할을 묻지 않는 3초 가입" 원칙 |
| 성적 OCR 학생 셀프 업로드 | janus_ocr_input_v1 | 1~2세션 | LlmProvider(비전) — API 이미 존재(admin용) | 학생 권한 개방 + janus_score 즉시 연동 |
| 알림 센터(웹) | janus_notifications_v1 | 1~2세션 | notification 모듈(존재) | 웹 student/teacher 페이지 이미 있음 — 시안 리스킨 수준 |
| 결제·구독 화면(멤버십 이벤트) | janus_payment_v1·membership_v1 | 3~4세션 | 가격 확정(N23)·PG(W9~10) | 이벤트 배너는 랜딩에 반영됨 |
| ~~하단 5탭 개편(홈/질문/진단/일정/내정보) — 모바일~~ | janus_app_v1 | — | — | **✅ 해소(N30, 2026-07-20)** — `App.tsx` 학생 탭 `['h','c','dg','b','d']` = 홈·질문·진단·일정·내정보. 탭에서 빠진 선생님찾기·강의실·자료실·커뮤니티는 **홈 바로가기로 보존**(기능 보존 원칙). 2026-07-30 expo web 실측 재확인 |

## 3. 배치표 트랙 연동 대기 (플랫폼측 준비 완료)

| 항목 | 플랫폼 상태 | 배치표 트랙에 남은 것 |
|---|---|---|
| C1 성적 자동연동 | `GET /scores/janus-score` + 허브 localStorage/`janus:score` 주입 완료 | 정적 HTML ingest 스니펫 부착(v22 규약 그대로) |
| C2 SSO 게이트 | `/sso/token`·`/sso/verify`·레지스트리(`baechipyo` 시드)·epoch 완료 | `?sso=` 검증 스니펫 + 티어 빌드(무료판 물리 분리) |
| C5 janus_report | — (W4 격차 리포트 v1에서 첫 사용 예정) | evidence(relTier 필수) 출력 규약 구현 |
| 허브 격차 탭 실엔진 연결 | 허브 iframe 서빙 동작 중(현재 격차 리포트 v2 파일) | **[DEC] N29** — 파일 교체로 갈지 엔진 직결로 갈지 |

## 4. 처리 규칙

- 본 문서 항목은 **개발 착수 금지** — 주간 리뷰(일요일)에서 [DEC] 후 실행계획서에 주차 배치하고 이 표에서 지운다.
- 시안만 있고 백엔드가 없는 화면을 급히 열어야 하면: '준비 중' 상태로 UI 만 선공개(services 클리닉·강의 패턴 재사용).

## 배치표 허브 목표 시안 (참조) — janus_report_hub_v1

- 파일: `docs/design-integration/janus_report_hub_v1_reference.html` (사용자 제공 2026-07-15, UI chrome 시안·저작권 데이터 없음).
- 컨셉: "한 액자, 세 그림, 0초 전환" — GNB·타이틀·탭 고정, 내용 iframe 스왑+프리로드 (**현재 허브가 이미 구현**).
- 개발 마무리 단계 리스킨 시 반영: (a) **격차 리포트를 허브 탭으로 편입**(현재 별도 /placement/gap), (b) **지원 포트폴리오(◫) 탭 신규**(=N28 확정 후), (c) 컬러/타이포(#2f6fb3·#1e3550·Pretendard)·브레드크럼 정렬.
- 실제 배치표 표 내용은 엔진(저작권 HTML) iframe 몫 — 시안은 액자만 정의(C6 경계 일치).
