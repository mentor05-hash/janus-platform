# 디자인 통합 스텝 0 — 감사·중복 매트릭스

> 작성 2026-07-07 · 갱신 2026-07-13 · 지시서: `docs/20_exec/야누스_클로드코드_디자인통합_지시서_2026-07-07.md`
> 상태: **a항(apps/web) 완료 · b항(야누스_사이트)·c항(gen_jeongmil.cjs) 대기 — 코드 자산이 이 기기에 없음(반입 필요)**.
> 규칙: defer 판정 항목은 스텝 1~3에서 건드리지 않는다. 이 문서가 절충의 근거.
>
> **2026-07-13 진행 기록 — 시안 번들 반입 + 스텝 1 완료 + 전면 리스킨 실행**
> - 사용자 업로드로 시안 번들 125파일 확보(janus_*.dc.html — 세션 스크래치에서 토큰 추출, repo 반입 안 함).
> - **스텝 1 완료**: `packages/brand/`(tokens.json·tokens.css·logo 2종·demo.html) + branding.config 야누스 값 + apply-branding.mjs TOKENS 주입 확장. 추출 기록 `01_토큰_추출_2026-07-13.md`.
> - **웹 전면 리스킨**: tokens.css/components.css 야누스 교체(레거시 별칭 유지), 하드코딩 hex 202건 매핑(40파일), 레이아웃 3종 아치 로고. 다크모드 야누스 다크 팔레트.
> - **모바일 전면 리스킨**: theme.ts LIGHT/DARK를 TOKENS 참조로 재작성(+신호등·AI·골드 확장 필드), 화면 리터럴 59건 매핑(14파일), 하단 탭 아이콘+활성 블루.
> - **신규 페이지(사용자 지시 — 시안 전용 화면 구현)**: `/`(관문 랜딩, home_v2 딥 포탈), `/placement`(배치표 무료 티어 UI — **예시 데이터만**, 저작권 데이터 반입 금지 준수), `/student`(나의 관문 대시보드), 로그인 리스킨. 구 랜딩은 `/legacy` 병행(원칙 ③).
> - 남은 것: b·c항 감사(코드 반입 대기), 스텝 2(배치표 v23 생성기 — c항 이후), `/services` 7종 rebuild, `__ITALL_TOKEN` 전역 식별자 rename(웹·모바일 동시 교체 필요 — 로직 접점이라 보류), 모바일 앱 아이콘 에셋 재렌더(`CHROME_PATH=<chromium> node scripts/apply-branding.mjs`).

---

## 1a. apps/web 감사 (완료)

### 라우트 현황

| 구역 | 라우트 | 비고 |
|---|---|---|
| 공개(마케팅) | `/` 랜딩 · `/services` · `/services/:slug`(7종) | **iframe 방식** — `public/site/*.html` 정적 서빙(SitePage.tsx 래퍼). 7/6 이식분 |
| 공개(계정·법률) | `/login` `/signup` `/forgot` `/terms` `/privacy` | LoginPage에 데모모드 자동로그인(`?u=&p=`) |
| 공개(도구) | `/consulting/apply`(컨설팅 신청 실폼) · `/room`(독립 룸) · `/embed/session` | 화이트라벨 분리 링크 |
| 선생님 `/app/*` | dashboard·inbox·bookings(+note)·schedule·evaluations·reverse·qna·payroll·materials·records·profile·classes·notifications·students/:id/notes·legal | Protected(teacher) + AppLayout |
| 관리자·HR `/admin/*` | dashboard·students·hr-teachers·hr-staff·reverse·policy·rooms·block·infra·reports·announcements·org·categories·member-types·evaluation·analytics·assignment·audit·payroll·scores·schedules·membership | Protected(admin,hr) + AdminLayout |
| 학생 `/student/*` | bookings·search·credits·reverse·auto-assign·qna·community·scores·… | Protected(student) + StudentLayout |

### 전역 스타일·테마 구조

- **토큰 소스 2층**: ① `apps/web/src/styles/tokens.css`(CSS 변수 전량 — teal 스케일·잉크/뉴트럴·상태칩·등급·그림자) + `components.css` ② `branding.config.json` → `scripts/apply-branding.mjs` → `apps/{web,mobile}/src/branding.generated.ts` → `main.tsx`가 런타임에 `--teal`·`--teal-900`·`--teal-500` 등 **CSS 변수 덮어쓰기**.
- 즉 **화이트라벨 주입 파이프라인이 이미 존재** — 스텝 1(토큰 단일 소스)은 이 구조를 재사용하는 것이 최단 경로: `branding.config.json` 색을 야누스 값(네이비 `#24405f`·골드 `#cf9a3a`·블루 `#2f6fb3`·실버 `#c7ccd2`)으로 교체 + tokens.css에 신호등 4구간·AI 라벨 3색 변수 추가 + `packages/brand/`로 승격(웹=import, 생성기=인라인 주입).
- 모바일: `apps/mobile/src/theme.ts`(Palette) + 동일 branding.generated 주입 — 같은 파이프라인에 물려 있음.

### 구 브랜드 잔재 grep (제거 대상 전수 목록)

| 위치 | 내용 | 처리 |
|---|---|---|
| `apps/web/public/site/*.html` 9파일 | "잇올" 명칭·로고 워드마크·구 팔레트(teal/gold) 그 자체 — 마케팅 정적 전체 | **rebuild**(스텝 3: 신규 랜딩 디자인으로 교체. 생성기 `apps/web/scripts/gen-marketing.mjs`도 야누스 데이터로 재작성) |
| `apps/web/src/pages/SitePage.tsx` | 주석·title 문자열 "잇올" | reskin(문자열 교체) |
| `apps/web/src/styles/tokens.css` | 주석 "Itall Foundations", teal 변수명(값은 주입식) | **keep 구조 / reskin 값** — 변수명 `--teal`은 하위호환 유지, 값만 야누스 블루로(또는 별칭 추가) |
| `branding.config.json` | 이미 중립화("멘토링 플랫폼"·com.example) — 잇올 명칭 없음 | keep — 야누스 값 기입만 |
| `apps/mobile` branding.generated | 동일 주입식 | keep — 재생성으로 해결 |

### 화면×결정 매트릭스 (apps/web 부분)

| 화면/영역 | 기존 상태 | 시안 대응 요소 | 결정 |
|---|---|---|---|
| `/` 랜딩 | 잇올 마케팅(iframe 정적) | 관문 히어로 4방향+론칭 랜딩(근거4종·신호등 카피) | **rebuild** (스텝 3, W3 결합 — 로직 없음, 전면 교체 안전) |
| `/services*` 7종 | 잇올 연계서비스 소개 | 야누스 서비스 체계(진단·Q&A·상담·과외·클리닉) | **rebuild** (동일 생성기 패턴 재사용, 데이터만 야누스로) |
| 로그인/회원가입 | 기능 완결, 구 팔레트 | 브랜드 가이드 토큰 | **reskin** (토큰 교체로 자동 반영 — 코드 불변) |
| 학생·선생님·관리자 콘솔 전체 | 기능 완결(예약·상담·Q&A·정산…) | 시안 P1·P2(대시보드·선생님 홈 등) | **keep**(이번 범위 밖) + 토큰 교체에 따른 자동 reskin만. P1/P2 목업은 **defer→백로그** |
| `/consulting/apply` | 실폼(API 연동) | 시안 컨설턴트 콘솔 | **keep**(로직) — 콘솔은 defer |
| `/room`·강의실 | 실시간 기능 완결 | 강의실 학생 화면 스킨(P2) | **keep** — defer |
| 브랜딩 파이프라인 | branding.config→generated→CSS 변수 | 브랜드 가이드(토큰·로고 규정) | **keep 구조** — 스텝 1에서 값 교체 + packages/brand 승격 |

## 1b. 야누스_사이트/*.html 감사 — ⛔ 대기

- **이 기기에 코드 부재**(전체 디스크 검색 0건). 문서상 확인: `index.html`에 의도분류·신호추출·LLM 훅(`JANUS_LLM_ENDPOINT` 폴백 포함)·커리큘럼 카드 로직.
- 반입 후 조사 항목: 재사용 가능 함수 경계, LLM 훅 인터페이스, 커리큘럼 카드 데이터 구조 → keep(함수 보존) / 표현만 rebuild.

## 1c. 배치표_DDD_v2/tools/gen_jeongmil.cjs 감사 — ⛔ 대기

- **이 기기에 코드 부재.** 반입 후 조사: 로직부(3모드 환산·gbias·relTier·janus_score)와 HTML 템플릿부의 **라인 경계**, TIER 인자 현재 구현 상태.

## 2. 배치표 기능 패리티 체크리스트 (초안 — v23 전환 게이트)

v22(기존) 대비 v23(신규 디자인)이 전 항목 통과해야 기본값 전환. 각 항목 실측 증빙(캡처/grep) 기록.

- [ ] 입력 3모드 (표점/백분위/등급 — 문서 기준, c항 감사 후 정확 명칭 확정)
- [ ] 버튼/실시간 토글
- [ ] 고속 ↔ 실측 토글
- [ ] gbias 반영
- [ ] 추천 4구간(안정/적정/소신/상향) + 성향
- [ ] relTier 배지
- [ ] 3개년 추세
- [ ] janus_score 자동연동(URL 파라미터 ingest)
- [ ] 워터마크·면책·"예시/재배포 금지" 고지
- [ ] 한글 패치 규칙 준수(python replace/heredoc만 — 정규식 금지)
- [ ] (v23 신규) 무료 티어 게이팅: 무료판 HTML에 회원 이상 데이터 **물리 부재** grep 증빙
- [ ] node --check 통과 + 3모드 입력 E2E 스크린샷

## 3. 다음 액션

1. **[사용자] 배치표_DDD_v2 · 야누스_사이트 코드 반입** → 1b·1c 감사 완료 → 본 문서 갱신·스텝 0 종결.
2. **[사용자] 7/8 클로드 디자인 실행** → 산출물 `40_workbench/design/`(영문 파일명) → 스텝 1 착수 가능.
3. 스텝 1은 기존 branding 파이프라인 재사용 경로로(위 1a 결론).
