# 무료판 배포 전 점검표 (W2 D6 [DOC])

> 작성 2026-07-13 · 대상: 무료 배치표(janus-public `/baechi/`)·플랫폼 공개 화면(/,`/placement`,`/services`).
> **배포 ≠ 런칭** — 공개 홍보는 W4 법률(저작권) 게이트 통과 뒤(핸드오프 재통합 C단계와 동일 원칙).
> 사용법: 배포 직전 전 항목 체크 + 증빙(grep 로그·캡처) 링크를 우측에 기입.

## A. 저작권·데이터 (위반 = 배포 중단)

- [ ] **무료판 HTML에 원본 데이터 물리 부재**: `grep -c "어디가\|esteacher\|고속\|INTERNAL"` 0건 + 실측 컷 수치 샘플 검사(구간 대표 외 노출 없음)
- [ ] repo/산출물에 §1-2 금지 파일 0건 (`git ls-files | grep -E 'adiga|esteacher|고속|calib'` 0건)
- [ ] `JANUS_DATA_DIR` 외 경로 하드코딩 0건 (`grep -rn "janus-data\|배치표_DDD" apps/ packages/` 0건)
- [ ] 허브(placement-hub): manifest 에 무료판 외 파일이 **공개 환경에 미배치**(회원급은 티켓 게이트 확인 — C2)
- [ ] gitleaks(또는 수동 시크릿 grep) 통과

## B. 고지·면책 (전 화면)

- [ ] 워터마크 + "모든 수치는 추정치·결과 보장 없음" 면책 문구
- [ ] "개인 진학지도용 — 무단 캡처·재배포 금지" 고지
- [ ] 예시 데이터 화면에 "예시" 라벨(랜딩 강좌·공지·지표, /placement 샘플)
- [ ] 출처 표기 원칙(아우구르 방법론 §3): "대학 발표 입결(어디가)·평가원 채점 통계·당해 모집요강" — 제3자 도구명 미표기
- [ ] '잇올' 문자열 0건 (`grep -rn "잇올" 산출물` — 핸드오프 재통합 A단계)

## C. 기능·성능

- [ ] 무료판 입력 제한·구간 대표 N개 노출 규칙 동작(N24 확정값 반영)
- [ ] node --check(임베드 스크립트) + 3모드 입력 E2E 캡처
- [ ] 모바일 375px 가독(헤드라인 대비) + 라이트하우스 성능 1회
- [ ] `?sso=` 없는 접근 = 무료판 모드(C2 — 토큰 없으면 무료판 필수)

## D. 계측·운영

- [ ] 간이 접속로그/전환 계측 동작: `funnel_event` 적재 확인(page `baechi`·cta `consult-reserve` — C3 왕복)
- [ ] `/api/v1/health` 정상 + 에러 시 폴백(관문 길찾기 규칙 폴백 등) 확인
- [ ] 백업(§2-4) 최근 성공 로그 확인
- [ ] 배포 후 롤백 경로 기록(URL 불변 원칙 — C4: `/baechi/` 교체 방식)

## E. 서명

| 점검일 | 점검자 | 결과 | 증빙 링크 |
|---|---|---|---|
| | | | |
