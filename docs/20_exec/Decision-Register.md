# 야누스 Decision-Register

> 생성 2026-07-07 · 잇올 Decision-Register(O1~O37 확정 / N1~N22 미결, `docs/90_archive` 및 구 작업폴더 참조)의 **번호 체계 승계** — 야누스는 **O38 / N23부터**.
> 규칙: 결정 발생 즉시 기록(세션 리추얼 ④). 미결(N)은 주간 리뷰에서 강제 처리 — "미결정 적체"가 1인 체제 최대 리스크(실행계획서 §6).

## 확정 (O)

| 번호 | 일자 | 결정 | 근거·비고 |
|---|---|---|---|
| O38 | 2026-07-07 | **저장소 3분리**: janus-platform(private, 모노레포)·janus-data(로컬 전용, remote 금지)·janus-public(public, Pages) | 실행계획서 §1-1. 저작권 데이터는 private repo도 불가 |
| O39 | 2026-07-07 | **10_platform 편입 = clone 방식**: 잇올 모노레포를 이력 보존 clone → `janus/10_platform/janus-platform`. 원본 폴더는 이름변경 보관(터널·경로 변경 허용) | 사용자 확정(7/7). 데모 스택 볼륨 재사용은 `COMPOSE_PROJECT_NAME=itall-mentoring` |
| O40 | 2026-07-07 | **문서 canonical = repo `docs/`**: `janus/00_docs`는 symlink. 살아있는 문서 1개 원칙 | 00_INDEX §2-3 |
| O41 | 2026-07-07 | **디자인 통합 절충 원칙 4 채택**: ①로직 기존·비주얼 신규 ②번들 직접 이식 금지 ③패리티 전 구버전 삭제 금지 ④신규 기능 백로그 | 디자인통합 지시서 §B |
| O42 | 2026-07-07 | **SSO 토큰 스키마·registry 확정**: rooms TokenService(HS256+epoch) 승격, sub/role/tier/aud/scope/epoch 클레임, `sso_service` 테이블 등록제, 정적 페이지는 `GET /sso/verify` 위임 | `docs/SSO_토큰_일반화_설계_v1_2026-07-07.md` (W1 D3) |
| O43 | 2026-07-07 | **janus_score 규약 = v22 키 동결**(변경 금지, 하위호환 확장만): `{gye, nb\|kor/mat/tam1/tam2, eng, han}` + period·mode·근거배지 추가 필드. 플랫폼이 규약에 맞춰 export | `docs/janus_score_변환스펙_v1_2026-07-07.md` (W1 D3). gye 실값·3모드 명칭은 c항 감사 후 v1.1 |
| O44 | 2026-07-26 | **학생 첫 화면 = "나의 관문" 홈**: 로그인 랜딩을 예약목록 → 홈 대시보드(지금 위치→다음 할 일→바로가기 3단)로 변경 | UX 검토서(`docs/40_design/janus_home_ux_redesign_review_2026-07-26.md`) D-a, 사용자 권장안 채택 |
| O45 | 2026-07-26 | **Q&A·커뮤니티 1차 내비 통합**: "질문" 그룹 1개 + 내부 서브탭. 라우트 URL 불변 | 동 검토서 D-b. 커뮤니티 트래픽 독립 성장 시 재분리 여지 |
| O46 | 2026-07-26 | **1차 내비 5그룹 고정(홈·질문·진단·일정·내정보)** + 웹 모바일 뷰포트 하단 5탭(RN과 동일 문법). 새 서비스는 그룹·홈 카드로만 수용, 탭 증설 금지. 진단 그룹은 접근 플래그와 무관하게 상시 노출(유도 상태) | 동 검토서 D-c·§7-4. 알림은 상단 벨로 승격 |
| O47 | 2026-07-26 | **티어 게이트 방식 = 자체 RedemptionCode/SSO 시작, 컨설턴트 티어만 Cloudflare Access** (N25 해소) | 사용자 승인(권장안 채택). 근거: 외부 의존 0으로 시작(O42 SSO 스키마 이미 확정) · 컨설턴트는 원본 컷·gbias 취급이라 별도 접근 통제 필요. 구현은 W3 회원 티어 빌드에 귀속(백로그 B003) · CF Access 는 컨설턴트 온보딩 트리거 시(Phase 2) |
| O48 | 2026-07-26 | **무료 노출 범위 = 정책값(코드 아님)**: 단일 소스 `tier-policy.config.json`(생성기 빌드 시 읽음) + `system_setting('free_exposure_policy')` 런타임 override. 법률 회신 전 보수적 잠정치 = 구간별 3개·수치 마스킹·검색/상세 잠금·추세 0년. 안전선(구간별 5개) 초과는 서버가 거부 | N24 를 블로커에서 내리기 위한 구조 결정. 회신 후 배포 없이 조정 가능(`PUT /api/v1/admin/free-exposure`, 본사 관리자 한정, 감사 로그 필수). W2 D2 '3~5개' 범위의 하한 채택 — 늘리긴 쉽고 이미 공개한 범위는 되돌릴 수 없다 |
| O50 | 2026-07-26 | **유료 티어 가격 확정 = D 등급사다리교정** (N23 해소). 구독 **가격은 승계값 유지**(Standard 49,000 / Premium 89,000 / VIP 149,000 원·월), **부여 크레딧만 교정**: Standard 30,000 → **24,000**/주 · Premium 210,000/월 유지 · VIP 350,000 → **380,000**/월. 단품(배치표+입결 시즌 패스) **19,000원 / 2개월** | 사용자 확정(D안 채택). 근거·재현 `docs/20_exec/유료_티어_가격_결정_워크시트_v1_2026-07-26.md` + `ops/pricing-sim.mjs`(불변식 41건). **교정 이유**: 승계값에 등급 역전이 있었다 — 부여 기준 세션 단가 7,518 → 8,476 → 8,514원으로 상위 등급이 더 비싸 업그레이드 유인이 없었다. 배분 원가가 크레딧에 선형이라 상위를 더 주는 방식으로는 못 고친다(470,000크면 스트레스 마진 0.9%) → 하위를 덜 주는 방향으로 교정. 교정 후 사다리 9,397 → 8,476 → 7,842원, 3등급 모두 마진 30% 이상, 소멸 0% 스트레스에서도 전 등급 흑자. 반영: `prisma/seed.ts`(신규 DB) + `migrations/0049_membership_grant_n23.sql`(기존 DB, 구값에서만 이동 — 운영자 수정분 보존). 운영 중 변경은 `PATCH /hr/membership-grades/{id}` — 배포 불필요 |
| O49 | 2026-07-26 | **무료판 공개 경로 = GitHub Pages(공개 repo `janus-public`)로 시작**, Cloudflare 는 커스텀 도메인·R2 필요 시 전환 | O38 이 janus-public 을 public 으로 계획 → 공개 repo Pages 는 무료·계정 추가 없음. '맥이 꺼져도 서빙' 목표를 B014(CF 계정) 없이 달성. 공개 전 `ops/publish-gate.sh` 기계 검사 통과 강제 |

| O51 | 2026-07-26 | **AI 사용량 = 3층 정책**(B221): ①사용자별 한도(`SubjectQuota` — 남용 방지 일·권리 보장 월, **fail-open**) ②전역 일 상한(B008, **fail-closed**) + 매출 연동 용도 **예약분 분리** ③**정합 불변식** — 감당 못 하는 양의 권리는 판매 자체를 거부(`PUT /admin/grade-benefits` 쓰기 시점 검사). 초과 응답을 분리: 권리 소진 **402**(업그레이드 안내) · 남용 **429** · 전역 상한 **503** | B218 §7-① 모순 해소. 정책서 `docs/30_features/야누스_AI사용량_3층정책_v1_2026-07-26.md`. **부수로 라이브 결함 2건 수정**: `POST /reports` 사용자별 한도 부재(1인이 공유 예산 소진 가능) · AI 검토 실패가 신고 접수를 막던 순서(안전 기능이 비용 보호에 꺼짐 → best-effort 로 분리). 방향이 반대인 이유: 비용은 닫고(폭주 방지), 권리는 연다(이미 받은 돈의 약속을 인프라 사정으로 깨지 않는다). 실측 36건 |
| O52 | 2026-07-26 | **동시 예약 상한 = 센터 소유 · 더 엄격한 쪽 승리**(B222). 등급값은 선택적 추가 제한이며 **기본 전 등급 무제한**. 유효값은 `effectiveConcurrentBookings()` 가 확정하고 `GET /me/benefits` 가 그 값만 노출 | B218 §7-② 해소. 총 소비량은 크레딧이 이미 제한하므로 이 축의 본질은 **슬롯 선점 방지(운영 통제)** — 판매 축에서 내렸다. 센터가 조인 값을 등급 혜택이 뚫으면 안 되므로 엄격한 쪽이 이긴다 → "VIP 무제한"의 정확한 의미는 **센터 한도 안에서의 무제한**. 조사 중 확인: `limit_policy.reservation_limit` 도 집행되지 않는 죽은 필드였다(충돌이 아니라 둘 다 미집행) |

## 미결 (N) — 결정 대기

| 번호 | 기한 | 안건 | 상태 |
|---|---|---|---|
| N24 | ~~W3~~ → 법률 회신 후 | **무료 노출 수**(구간별 대표 N개) | **잠정치 적용 중**(O48 — 구간별 3개). 회신 후 확정. 블로커 아님 |
| N26 | 스텝 3 착수 전 | **관문 히어로 방향 택1**(딥 네이비 포탈 vs 여명 라인 — 미정 시 딥 포탈 가정) | 7/8 시안 후 |
| N27 | W8 | 리그 수치(승급 기준·2부 질문가·보상률·지정 가산) | 기획서 확정 후 운영 보정 |
| N28 | W8과 함께 | 포트폴리오(가·나·다 조합) 개발 여부 — 유료 티어 킬러 후보 | 백로그(스텝 4) |

## 승계 참조 (잇올 시절 주요 미결 중 야누스에 이어지는 것)

- N22 화이트보드 PDF 배경(룸 서비스) — 사업확장 후.
- O2/PG: 토스페이먼츠로 W9~10 실연동 예정(실행계획서).
