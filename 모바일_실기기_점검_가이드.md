# 모바일 실기기 점검 가이드 (학생·학부모 앱)

> Expo Go로 **실기기(Galaxy S23 / iPad mini)** 에서 학생·학부모 앱을 점검하는 절차.
> 계정·구독 없이 가능. 백엔드는 Mac/PC에서 구동, 모바일은 같은 와이파이에서 LAN 접속.
> 최종 점검: 2026-06-30 · expo-doctor 17/17 통과 · Mac LAN IP 예시 `192.168.100.119`

---

## 0. 요약
- 백엔드(Mac/PC) 구동 → 모바일 `apiBase`를 **Mac의 LAN IP**로 지정 → Expo Go로 QR 스캔.
- 점검 대상: **학생 흐름**(검색→슬롯→예약→크레딧) · **학부모 흐름**(자녀 대시보드 집계→상담기록→결제).

---

## 1. 사전 준비
1. **백엔드 구동**(Mac/PC):
   ```bash
   cd janus-platform
   docker compose up -d           # postgres + redis
   cd apps/api && npm run start:dev   # http://localhost:3000/api/v1
   ```
   헬스: `curl http://localhost:3000/api/v1/health` → `{"status":"ok"}`
2. **시뮬 시드 적용**(학생 20·학부모 15·연결 매트릭스):
   ```bash
   cd apps/api && npm run seed:sim
   ```
3. **CORS 허용**(`apps/api/.env`): `CORS_ORIGINS` 미설정이면 전체 허용(기본). 로컬 점검엔 그대로 OK.
4. **Mac LAN IP 확인**: `ipconfig getifaddr en0` (예: `192.168.100.119`).
5. **폰/패드 준비**: 스토어에서 **Expo Go** 설치(무료, 계정 불필요). Mac과 **같은 와이파이**.

---

## 2. 모바일 API 주소 설정 (필수)
`apps/mobile/app.json` 의 `extra.apiBase` 를 **localhost → Mac LAN IP** 로 변경:
```json
"extra": { "apiBase": "http://192.168.100.119:3000/api/v1" }
```
> 기기에서 `localhost` 는 기기 자신을 가리켜 접속 실패하므로, 반드시 Mac IP 로.

---

## 3. 실행
```bash
cd apps/mobile && npm ci && npx expo start
```
- 터미널 QR 코드를 **Expo Go**로 스캔(Android: Expo Go 내 스캔 / iOS: 카메라).
- 앱이 기기에서 로드되면 로그인 화면 표시.

> `expo-secure-store`(토큰 저장)만 사용 → **별도 dev build 불필요**, Expo Go로 충분.

---

## 4. 점검 체크리스트

### 4-A. 학생 앱 (로그인 `s1` / PW `dev-password!`)
| # | 동작 | 기대 |
|---|---|---|
| 1 | 로그인 | 학생 홈 진입(검색/크레딧 탭) |
| 2 | **검색** 탭 → 선생님 목록 | 강남센터 선생님(T1~T4) 표시, S급 뱃지 |
| 3 | 선생님 선택 → **슬롯** | 가용/휴게(rest)/예약(booked) 칩 구분 표시 |
| 4 | 슬롯 선택 → 예약 | 크레딧 차감, 예약 생성 |
| 5 | **크레딧** 탭 | 잔액·거래내역 표시 |

### 4-B. 학부모 앱 (로그인 `g1` / PW `dev-password!`)
| # | 동작 | 기대 |
|---|---|---|
| 1 | 로그인 | 학부모 홈(자녀/결제 탭) |
| 2 | **연결 자녀** | **시뮬학생1·2 카드 2개** + 각 카드에 **집계 배지**: `상담기록 N` · `미결제 M` ← 이번에 추가한 기능 |
| 3 | 자녀 카드 탭 | 해당 자녀 상담기록(공개필드만, **메모 비노출**) |
| 4 | 미결제>0 인 자녀 | `미결제` 배지가 **주황 강조** |
| 5 | **결제** 탭 | 자녀 결제요청 목록, 대납(pay) 동작 |

> **집계 배지 비어 보이면**(상담기록 0·미결제 0): 시드 학생은 기본 데이터가 없어 정상. 의미 있는 값 확인은 §5 참고.

### 4-C. 권한·격리(선택)
- `g1` 으로 **연결 안 된 학생** 기록 접근 시도 → 차단(403, 앱에서 조회 실패).

---

## 5. 집계 배지 비어있을 때 — 의미 있는 데이터 만들기
웹 관리자/선생님으로 자녀에게 상담기록·결제요청을 만들면 배지가 갱신됩니다:
1. 선생님 앱/웹(`t1`)으로 `s1` 예약 확정 → 상담기록 final 저장 → 완료 → 학부모 카드 `상담기록 1`.
2. 학생 `s1` 이 잔액 부족 상태로 예약 시도(또는 결제요청 생성) → 학부모 카드 `미결제 1`(주황).

---

## 6. 트러블슈팅
| 증상 | 원인/조치 |
|---|---|
| "네트워크 오류"·무한 로딩 | `apiBase` 가 `localhost` → **Mac LAN IP** 로 변경, 같은 와이파이 확인 |
| 로그인 실패 | 백엔드 미구동/시드 미적용 → `health` 확인, `npm run seed:sim` |
| CORS 차단 | `.env` `CORS_ORIGINS` 제거(전체 허용) 후 백엔드 재시작 |
| Expo Go 연결 안 됨 | Mac 방화벽이 19000/19001/8081 포트 차단 여부 확인, 같은 LAN |
| 토큰 만료 | 앱이 refresh 자동 처리. 안 되면 재로그인 |

---

## 시드 로그인 (PW 전원 `dev-password!`)
- 학생: `s1`~`s20` (강남 s1~8, 분당 s9~14, 잠실 s15~20; s16~20 보호자 없음)
- 학부모: `g1`~`g15` (g1→s1·s2 형제, g3·g4→s5 공동보호자 등)
- 선생님(웹/검색 노출): `t1`~`t10` (t1·t5·t8 = S급)

> 모바일은 학생·학부모 전용. 선생님·관리자·HR 은 웹(`apps/web`).
